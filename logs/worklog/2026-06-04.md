# 工作日志 2026-06-04

## 23:50 当前图 COM 执行反馈增强

- 按工作流执行同步：`git fetch`、`git pull --rebase`，远端已是最新。
- 安装默认 Python 的 `pywin32`，验证 `win32com.client` 可导入。
- 修复当前图执行兼容：
  - 增加 `AutoCAD.Application.26`，覆盖 AutoCAD 2027。
  - COM 失败时返回 `tried_prog_ids`。
  - COM 失败时检测 `acad.exe` 进程状态，`tasklist` 被拒绝时用 PowerShell `Get-Process` 兜底。
  - 任务记录保存 `send_result`，便于压缩上下文或换 AI 后继续排障。
- 新增 CLI 排障入口：
  - `task-error-detail`
  - `task_error_detail`
- 增强 `task_error_detail`：
  - 当 `ACAD_COM_UNAVAILABLE` 且 `acad_process.running = true` 时，返回 `acad_process_without_com` 诊断。
- 本机真实执行：
  - `scripts\current-smoke-test.cmd --execute`
  - 最新任务 `20260604-234553-07493147`
  - 结果仍为 `ACAD_COM_UNAVAILABLE`
  - 诊断显示 `acad.exe` 正在运行，但 COM 无法附着。
- 当前判断：
  - 不是缺 `pywin32`。
  - 不是缺 AutoCAD 2027 ProgID。
  - 下一步需要关闭 AutoCAD，用普通权限重开并打开测试 DWG 后复测。

### 验证

- `scripts\doctor.cmd`：通过；AutoCAD 2027 和 accoreconsole 可检测，当前 `acad.exe` 仍在运行。
- `python -m compileall src tests`：通过。
- `python -m yang_cad_agent.cli validate-lisp toolbox\plugins\current_smoke\main.lsp --track B`：通过。
- MCP stdio `{"action":"server_info"}`：通过。
- `scripts\current-smoke-test.cmd` dry-run：通过，生成 wrapper 和 completion marker 路径。
- `scripts\test.cmd`：普通沙箱因 Windows Temp ACL 失败；提升权限重跑通过，58 tests OK。

## 当前图一键烟测入口

- 新增 `scripts\current-smoke-test.cmd`。
- 默认 dry-run，不连接 AutoCAD。
- `--execute` 模式只调用 `current-lisp --script toolbox\plugins\current_smoke\main.lsp --execute`。
- 脚本会解释 `completed`、`sent_unconfirmed`、`failed`，并提示把 `task_id` 交给 AI 用 `task_error_detail` 排障。
- dry-run 实测通过，任务 `20260604-224020-76802b31`。
- execute 失败路径实测通过，任务 `20260604-224027-0b90ef9e` 返回 `ACAD_COM_DEPENDENCY_MISSING`；当前 Python 环境缺 pywin32，未修改 DWG。
- 最终验证：
  - `scripts\current-smoke-test.cmd` dry-run 通过。
  - `scripts\current-smoke-test.cmd --execute` 在 AutoCAD 已打开但 Python 缺 pywin32 时返回 `ACAD_COM_DEPENDENCY_MISSING`，未修改 DWG。
  - `scripts\doctor.cmd` 通过；检测到 AutoCAD 2027 正在运行。
  - `scripts\test.cmd` 沙箱内因系统 Temp ACL 失败；提权重跑通过，55 个测试 OK。
  - `compileall src tests` 通过。
  - MCP `server_info` 通过，仍为 10 个工具。
  - `current_smoke` Track B LISP 校验通过。
- 更新文档：
  - `docs\USER_GUIDE.md`
  - `docs\CAD_LOCAL_TESTS.md`
  - `docs\PROJECT_STATE.md`
  - `handoff\CURRENT_HANDOFF.md`

## MCP stdio 兼容描述层

- 新增 `src\yang_cad_agent\mcp_manifest.py`。
- MCP stdio 新增动作：
  - `server_info`
  - `manifest`
- 这两个动作返回机器可读的能力描述，方便 Codex/Antigravity 在调用前确认：
  - 当前 transport：`stdio-json-lines`
  - 当前不是官方 MCP SDK server：`official_mcp_sdk: false`
  - 启动入口：`python -m yang_cad_agent.mcp_stdio`、`yang-cad-mcp-stdio`
  - 当前工具清单和数量
  - 安全边界：不暴露泛用 shell、`health_check` 强制 dry-run、`rollback_dry_run` 只预演、真实批量执行必须走 CLI 确认

## 验证

- `tests.test_mcp_manifest tests.test_mcp_stdio` 通过，9 个测试。
- `compileall src tests` 通过。
- `scripts\doctor.cmd` 通过：
  - AutoCAD 2027 `accoreconsole.exe` 已发现。
  - `acad2027.cfg` 存在且非只读。
  - 当前没有 AutoCAD 进程占用。
- 全量 `unittest discover -s tests` 通过，50 个测试。
- MCP stdio `server_info` 实测通过，返回 9 个工具、启动入口和安全边界。

## 下一步

1. 跑完验证并修复发现的问题。
2. 提交并推送。
3. 下一轮优先做“最短可用路径”：让用户可以一键跑 sample 图纸 dry-run 体检并看到报告。

## 个人版一键体检入口

- 新增 `src\yang_cad_agent\personal_health.py`。
- 新增 CLI 命令 `personal-health`。
- 新增 Windows 入口 `scripts\personal-health-check.cmd`。
- 新增 MCP stdio 工具 `personal_health`，固定 dry-run。
- 新增轻量版 `CLAUDE.md`，避免其他 AI 将项目误切到大型 C# / WPF 插件路线。
- 新增 `.agent/reports/` gitignore，避免提交本机报告产物。
- 更新 `docs\USER_GUIDE.md` 和 `docs\SAMPLE_DWGS.md`。

## 验证补充

- `tests.test_personal_health tests.test_mcp_stdio tests.test_mcp_manifest` 通过，12 个测试。
- 全量 `unittest discover -s tests` 通过，53 个测试。
- `compileall src tests` 通过。
- `scripts\doctor.cmd` 通过。
- MCP stdio `server_info` 通过，当前返回 10 个工具。
- `scripts\personal-health-check.cmd` 实测通过：
  - 默认扫描 `sample`。
  - 匹配 11 个 DWG。
  - 三个只读报告插件 dry-run 成功。
  - 输出 `.agent\reports\PERSONAL_HEALTH_PLAN.md`。

## 测试副本真实只读体检和新增报告插件

- 新增插件：
  - `toolbox\plugins\batch_xref_image_report`
  - `toolbox\plugins\batch_title_block_candidate_report`
- `health-check` / `personal-health` 现在包含 5 个只读报告插件。
- `summarize-reports` 新增外参/图片引用、图框标题栏候选两个报告区块。
- `personal-health` dry-run 报告新增下一步可执行命令。
- 已刷新测试副本：`sample\S001.dwg` -> `.agent\tmp\sample-run\S001-test.dwg`。
- 已执行真实只读体检：`scripts\personal-health-check.cmd .agent\tmp\sample-run --execute`。
- 5 个插件全部成功，并生成：
  - `S001-test.dwg.layer-report.csv`
  - `S001-test.dwg.block-report.csv`
  - `S001-test.dwg.annotation-report.csv`
  - `S001-test.dwg.xref-image-report.csv`
  - `S001-test.dwg.title-block-candidate-report.csv`
  - `CAD_REPORT_SUMMARY.md`
- 汇总结果：
  - 图层 12
  - 图层对象 93
  - 普通块参照 1
  - 文字/标注对象 24
  - 外参/图片/底图引用 4
  - 图框标题栏候选 0
- 回滚 dry-run 已验证：`20260604-214229-d4f6c53d`。
- 验证：
  - 相关单元测试通过，6 个测试。
  - `scripts\test.cmd` 通过，53 个测试。
  - 新增 LISP 插件静态校验通过。
  - `toolbox-list --json` 显示新增插件 manifest 有效。
## 当前图 LISP 执行反馈闭环

- 增强 `src\yang_cad_agent\current_lisp.py`：
  - dry-run 会生成 wrapper LISP。
  - 返回 `wrapper_path` 和 `completion_marker`。
  - execute 会发送 wrapper，而不是直接加载原始 LISP。
  - wrapper 执行后尽量写入 `.agent\current\<task_id>\result.json`。
  - execute 状态现在区分 `completed`、`sent_unconfirmed`、`failed`。
- 更新 `.gitignore`，忽略 `.agent/current/`。
- 更新 `tests\test_current_lisp.py`：
  - 覆盖 dry-run 返回完成标记。
  - 覆盖 wrapper LISP Track B 静态校验。
  - 覆盖 COM 发送成功但未看到标记时返回 `sent_unconfirmed`。
- 更新文档：
  - `docs\USER_GUIDE.md`
  - `docs\FEATURES.md`
  - `docs\PROJECT_STATE.md`
  - `handoff\CURRENT_HANDOFF.md`
- 已验证：
  - `tests.test_current_lisp` 通过。
  - `current_smoke` Track B LISP 校验通过。
  - dry-run 生成的 wrapper Track B LISP 校验通过。
## 当前图 LISP 执行反馈最终验证

- `scripts\doctor.cmd` 通过，AutoCAD 2027 accoreconsole 可用，当前无 AutoCAD 进程。
- `scripts\test.cmd` 在沙箱内因系统 Temp ACL 报 `PermissionError`；提权重跑同一命令通过，55 个测试 OK。
- `compileall src tests` 通过。
- MCP stdio `server_info` 通过，当前仍为 10 个工具，未新增真实执行类 MCP 工具。
- `current_smoke` 和 dry-run 生成的 wrapper 均通过 Track B LISP 静态校验。
