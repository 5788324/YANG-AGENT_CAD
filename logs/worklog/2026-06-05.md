# 工作日志 2026-06-05

## 00:10 guarded 当前图烟测入口

- 按工作流执行：
  - `git status --short --branch`
  - `git fetch`
  - `git pull --rebase`
  - 阅读 `handoff\CURRENT_HANDOFF.md`
  - 阅读 `docs\PROJECT_STATE.md`
  - 查看最近 worklog
- 复测 `scripts\current-com-diagnose.cmd`：
  - `pywin32.available = true`
  - `python.elevated = false`
  - `acad_process.running = false`
  - `attachable = false`
- 新增 `src\yang_cad_agent\current_smoke.py`。
- 新增 CLI 命令 `current-smoke`。
- 更新 `scripts\current-smoke-test.cmd`：
  - dry-run 仍生成 wrapper 和 completion marker。
  - `--execute` 先做 COM ready check。
  - 如果 AutoCAD 未运行或 COM 不可附着，返回 `status: blocked`，不发送 LISP。
- 新增/更新测试：
  - `tests\test_current_smoke.py`
  - `tests\test_cli_commands.py`

### 本机验证

- `scripts\current-smoke-test.cmd` dry-run：通过，任务 `20260605-000828-eaf9e593`。
- `scripts\current-smoke-test.cmd --execute`：返回 `status: blocked`，`error_code: ACAD_COM_UNAVAILABLE`，未发送 LISP。
- `python -m unittest tests.test_current_smoke tests.test_cli_commands`：通过，7 tests OK。
- `scripts\doctor.cmd`：通过；当前无 AutoCAD 进程。
- `python -m compileall src tests`：通过。
- MCP stdio `{"action":"server_info"}`：通过，当前 `tool_count = 11`。
- `validate-lisp toolbox\plugins\current_smoke\main.lsp --track B`：通过。
- `scripts\test.cmd`：普通沙箱因 Windows Temp ACL 失败；提升权限重跑通过，67 tests OK。
## 00:55 AutoCAD 启动入口和 COM 实测

- 按工作流重新执行：
  - `git status --short --branch`
  - `git fetch`
  - `git pull --rebase`
  - 阅读 `handoff\CURRENT_HANDOFF.md`
  - 阅读 `docs\PROJECT_STATE.md`
  - 查看当天 worklog
- 新增 `src\yang_cad_agent\acad_launch.py`。
- 新增 CLI 命令 `acad-open`。
- 新增脚本 `scripts\open-autocad-test.cmd`。
- 安全策略：
  - 默认 dry-run，不启动 AutoCAD。
  - `--execute` 才启动 AutoCAD。
  - 默认打开 `.agent\tmp\current-open\S001-current-test.dwg` 测试副本。
  - 不自动关闭 AutoCAD，避免误关未保存图纸。
  - 不通过 MCP 暴露真实开关 CAD 能力。

### 本机验证

- `python -m unittest tests.test_acad_launch tests.test_cli_commands`：通过，7 tests OK。
- `scripts\open-autocad-test.cmd` dry-run：通过，识别到 `C:\Program Files\Autodesk\AutoCAD 2027\acad.exe`。
- `scripts\open-autocad-test.cmd --execute`：已启动 AutoCAD 2027，PID `56860`，打开 `.agent\tmp\current-open\S001-current-test.dwg`。
- 启动后两次运行 `scripts\current-com-diagnose.cmd`：
  - `acad_process.running = true`
  - `attachable = false`
  - `AutoCAD.Application.26` 返回 `操作无法使用`
- `scripts\current-smoke-test.cmd --execute`：返回 `status: blocked` / `ACAD_COM_UNAVAILABLE`，未发送 LISP。
- 结束前验证：
  - `scripts\doctor.cmd`：通过；提示 AutoCAD 进程 `56860` 正在运行。
  - `python -m compileall src tests`：通过。
  - MCP `server_info`：通过，`tool_count = 11`。
  - `validate-lisp toolbox\plugins\current_smoke\main.lsp --track B`：通过。
  - `scripts\test.cmd`：普通沙箱仍因 Windows Temp ACL 失败；提升权限重跑通过，71 tests OK。

## 01:20 COM 深度诊断

- 增强 `src\yang_cad_agent\acad_com_diagnose.py`：
  - 输出 `acad_process_details`。
  - 输出 `registered_prog_ids`。
  - 输出 COM Running Object Table 信息 `rot`。
  - 新增诊断规则 `acad_not_in_running_object_table`。
- 更新 `tests\test_acad_com_diagnose.py`。
- 更新 `scripts\current-com-diagnose.cmd` 的结果提示。

### 本机验证

- `python -m unittest tests.test_acad_com_diagnose tests.test_cli_commands`：通过，7 tests OK。
- `python -m compileall src tests`：通过。
- `scripts\current-com-diagnose.cmd`：通过，返回：
  - `AutoCAD.Application.26` 已注册。
  - `acad.exe` PID `56860` 正在运行。
  - `MainWindowTitle` 为空。
  - ROT `entry_count = 0`。
  - `attachable = false`。
  - 命中 `acad_not_in_running_object_table`。
- 修复提升权限测试时暴露的 PowerShell 输出编码容错问题：`_acad_process_details()` 现在使用 `encoding="utf-8"` 和 `errors="replace"`，并允许 `stdout` 为空。
- `scripts\test.cmd`：普通沙箱因 Windows Temp ACL 失败；提升权限重跑通过，72 tests OK。

## 继续工作流：AutoCAD 窗口探测

- 按工作流执行：
  - `git status --short --branch`
  - `git fetch`（首次 TLS 握手失败，提升权限重试成功）
  - `git pull --rebase`
  - 阅读 `handoff\CURRENT_HANDOFF.md`
  - 阅读 `docs\PROJECT_STATE.md`
  - 查看当天 worklog
- 尝试使用 Computer Use 观察 AutoCAD 界面，但当前会话返回：
  - `Computer Use native pipe is unavailable: Unsupported local Computer Use helper method: list_windows`
- 改为增强本地只读诊断：
  - `acad-com-diagnose` 新增 `window_probe`。
  - 新增诊断规则 `acad_process_without_top_level_window`。
  - 新增诊断规则 `acad_windows_not_visible`。
  - 新增诊断规则 `acad_visible_window_title_empty`。
- 更新 `scripts\current-com-diagnose.cmd`，当出现无顶层窗口时提示不要发送 LISP，也不要在未确认无未保存图纸前关闭进程。
- 更新文档：
  - `docs\CAD_LOCAL_TESTS.md`
  - `docs\USER_GUIDE.md`
  - `docs\PROJECT_STATE.md`
  - `handoff\CURRENT_HANDOFF.md`

### 本机验证

- `python -m unittest tests.test_acad_com_diagnose`：通过，4 tests OK。
- `python -m unittest tests.test_acad_com_diagnose tests.test_cli_commands`：通过，8 tests OK。
- `python -m compileall src tests`：通过。
- `scripts\doctor.cmd`：通过；仍提示 AutoCAD PID `56860` 正在运行。
- MCP `server_info`：通过，`tool_count = 11`。
- `validate-lisp toolbox\plugins\current_smoke\main.lsp --track B`：通过。
- `scripts\current-com-diagnose.cmd`：通过，返回：
  - AutoCAD PID `56860` 仍在运行。
  - `window_probe.window_count = 0`。
  - ROT `entry_count = 0`。
  - `attachable = false`。
  - 命中 `acad_process_without_top_level_window`、`acad_not_in_running_object_table`、`acad_process_without_com`。
- `scripts\test.cmd`：普通沙箱因 Windows Temp ACL 失败；提升权限重跑通过，73 tests OK。
