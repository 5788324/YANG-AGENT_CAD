# 工作日志 2026-06-03

## MCP 失败任务排障包工具

- 新增 MCP stdio 工具 `task_error_detail`。
- 新增 `src\yang_cad_agent\task_query.py` 的 `error_detail(...)`：
  - 读取指定任务记录。
  - 返回错误码、任务状态、回滚可用状态。
  - 收集 `.agent\logs\<task_id>\*.log`。
  - 返回每个日志的尾部内容，避免一次性输出过长日志。
  - 对可回滚任务只执行 `rollback_task(..., dry_run=True)`。
- 更新测试：
  - `tests\test_mcp_stdio.py`
  - `tests\test_task_query.py`
- 更新文档：
  - `docs\MCP.md`
  - `docs\FEATURES.md`
  - `docs\PROJECT_STATE.md`
  - `handoff\CURRENT_HANDOFF.md`
- 新增 `.gitignore` 忽略 `tmp-tests/`，避免本地测试临时目录进入 Git。

## 验证

- `tests.test_mcp_stdio` 通过。
- `compileall src tests` 通过。
- `scripts\doctor.cmd` 通过。
- MCP stdio `list_tools` 实测通过，能看到 `task_error_detail`。
- MCP stdio `task_error_detail` 实测通过：
  - 任务：`20260524-140406-3c2fbf05`
  - 错误码：`LISP_LOAD_FAILED`
  - 回滚预演：`dry_run: true`
  - 日志尾部包含：`文件加载已取消:D:/codex/Yang Agent_CAD/toolbox/plugins/batch_layer_report/main.lsp`

## 已知限制

- 全量 `unittest discover` 在当前 Codex 沙箱中被临时目录写权限拦截，错误为 `PermissionError`。
- 该限制发生在测试创建临时目录后写入文件时，不是 `task_error_detail` 的功能断言失败。
- `doctor` 检测到当前有 `acad.exe` 进程运行；真实批量执行前应关闭 AutoCAD。

## MCP 错误码结构化解释

- 新增 `src\yang_cad_agent\error_codes.py` 中的 `ERROR_DETAILS` 和 `explain_error_code(...)`。
- 增强 `task_error_detail`：
  - 保留旧字段 `error_code`。
  - 新增 `error.code`。
  - 新增 `error.meaning`。
  - 新增 `error.suggestion`。
  - 新增 `error.severity`。
- 未知错误码会保留原始 code，并回退到未分类错误解释。
- 更新测试：
  - `tests\test_task_query.py` 覆盖已知错误码解释。
  - `tests\test_task_query.py` 覆盖未知错误码回退。
- 更新文档：
  - `docs\MCP.md`
  - `docs\FEATURES.md`
  - `docs\PROJECT_STATE.md`
  - `handoff\CURRENT_HANDOFF.md`

## 验证补充

- 普通沙箱运行 `tests.test_task_query tests.test_mcp_stdio` 时，`tests.test_task_query` 仍因临时目录写权限失败。
- 提权运行同一测试命令通过：9 个测试通过。
- `compileall src tests` 通过。
- `scripts\doctor.cmd` 通过。
- MCP stdio 实测 `task_error_detail` 返回结构化 `error` 字段。

## 错误码文档一致性校验

- 新增 `tests\test_error_codes.py`。
- 测试目标：
  - 错误码常量必须全部写入 `ERROR_DETAILS`。
  - 错误码常量必须全部写入 `docs\ERROR_CODES.md`。
  - `ERROR_DETAILS` 中每个条目必须有 `meaning`、`suggestion`、合法 `severity`。
- 修复文档缺口：
  - 补充 `ACAD_COM_DEPENDENCY_MISSING` 到 `docs\ERROR_CODES.md`。
- 验证：
  - `tests.test_error_codes` 通过。
  - 全量 `unittest discover -s tests` 通过，当前 38 个测试。
  - `compileall src tests` 通过。

## MCP 日志关键字诊断

- 增强 `task_error_detail`，新增 `diagnostics` 字段。
- 诊断规则：
  - `acad_startup_noise`
  - `lisp_load_canceled`
  - `acad_config_locked`
  - `referenced_file_missing`
  - `no_log_rule_match`
- 更新测试：
  - `tests\test_task_query.py` 覆盖 LISP 加载取消诊断。
  - `tests\test_task_query.py` 覆盖配置文件锁定诊断。
  - `tests\test_task_query.py` 覆盖未知错误码保底诊断。
- 验证：
  - 普通沙箱运行涉及临时目录的测试仍因权限失败。
  - 提权运行 `tests.test_task_query tests.test_mcp_stdio` 通过，当前该组 10 个测试。
  - MCP stdio 实测任务 `20260524-140406-3c2fbf05` 返回 `acad_startup_noise` 和 `lisp_load_canceled`。
  - `compileall src tests` 通过。

## 日志诊断规则模块化

- 新增 `src\yang_cad_agent\log_diagnostics.py`。
- 从 `src\yang_cad_agent\task_query.py` 移出日志诊断规则。
- 新增 `tests\test_log_diagnostics.py`。
- 保持 `task_error_detail` 返回格式不变，仍返回 `diagnostics`。
- 验证：
  - `tests.test_log_diagnostics tests.test_mcp_stdio` 通过，11 个测试。
  - 提权运行 `tests.test_task_query tests.test_log_diagnostics tests.test_mcp_stdio` 通过，15 个测试。
  - MCP stdio 实测 `task_error_detail` 仍返回 `acad_startup_noise` 和 `lisp_load_canceled`。
  - `compileall src tests` 通过。
